[package]
name = "voxel_bevy"
version = "0.1.0"
edition = "2024"

[dependencies]
voxel_core = { path = "../voxel_core", features = ["bevy"] }
bevy = "0.18"
bevy_mesh = "0.18"
bevy_common_assets = { version = "0.16", features = ["ron"] }
serde = { version = "1", features = ["derive"] }
image = { version = "0.25", default-features = false, features = ["png"] }
wgpu-types = "27.0.1"
