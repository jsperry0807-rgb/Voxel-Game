[package]
name = "time"
version = "0.1.0"
edition = "2024"

[dependencies]
glam = { version = "0.32.1", features = ["serde"] }
bevy = "0.18"
core = { path = "../core", package = "core" }
serde = { version = "1.0", features = ["derive"] }
